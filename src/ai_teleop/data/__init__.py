"""Data-generation pipeline and dataset loaders.

Runs episodes (scripted noisy-human + expert + controller + sim), logs structured
trajectories to disk, and provides loaders for BC training. To be populated in
Milestone 4.
"""
