"""Evaluation harness.

Passive observer that watches the runtime, decides when trials start/end,
classifies success/failure, and computes the KPIs defined in project-scope.md.
Has no upstream dependency from the controller (Dependency Inversion). To be
populated post-Milestone 4.
"""
