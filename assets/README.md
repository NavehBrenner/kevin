# assets/

Static project assets — MJCF (MuJoCo) scene files, meshes, textures.

```
mjcf/
├── menagerie_panda/    # Franka Panda + Franka Hand from mujoco_menagerie (vendored)
├── wall_with_holes.xml # Custom: vertical wall with chamfered holes
├── peg.xml             # Custom: cylindrical peg geometry
└── full_scene.xml      # Combined: panda + wall + peg + lighting + wrist camera
```

To be populated in Milestone 1.
